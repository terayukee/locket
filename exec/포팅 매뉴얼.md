# 포팅 매뉴얼

## 목차

1. [개발환경](#개발환경)
2. [ec2-포트-번호](#ec2-포트-번호)
3. [환경변수](#환경변수)
    - [API Gateway 기본 설정](#locketapigateway-applicationyml)
    - [API Gateway 개발환경 설정](#locketapigateway-application-devyml)
    - [API Gateway 운영환경 설정](#locketapigateway-application-prodyml)
    - [Discovery Service 기본 설정](#locketdiscovery-applicationyml)
    - [Discovery Service 개발환경 설정](#locketdiscovery-application-devyml)
    - [Discovery Service 운영환경 설정](#locketdiscovery-application-prodyml)
    - [Payment Service 기본 설정](#locketpaymentservice-applicationyml)
    - [Payment Service 개발환경 설정](#locketpaymentservice-application-devyml)
    - [Payment Service 운영환경 설정](#locketpaymentservice-application-prodyml)
    - [User Service 기본 설정](#locketuserservice-applicationyml)
    - [User Service 개발환경 설정](#locketuserservice-application-devyml)
    - [User Service 운영환경 설정](#locketuserservice-application-prodyml)
    - [User Service Firebase-key 파일](#locketuserservice-firebase-keyjson)
    - [ElasticSearch Service 기본 설정](#locketelasticsearch-applicationyml)
    - [ElasticSearch Service 개발환경 설정](#locketelasticsearch-application-devyml)
    - [ElasticSearch Service 운영환경 설정](#locketelasticsearch-application-prodyml)
    - [AI Service 환경 변수 설정](#locketaiservice-env)
    - [AI Service 의존성 파일](#locketaiservice-requirementstxt)
4. [CI/CD 구축](#cicd-구축)
    - [Jenkins 설치 플러그인](#jenkins-설치-플러그인)
    - [Jenkins Credentials 설정](#jenkins-credentials)
    - [Pipeline](#pipeline)
        - [Jenkins File - FastAPI - LocketAIService](#backend-fastapi---locketaiservice-pipeline)
        - [Docker File - FastAPI - LocketAIService](#backend-fastapi---locketaiservice-pipeline-docker)
        - [Jenkins File - SpringBoot - LocketAPIGateway](#backend-springboot---locketapigateway-pipeline)
        - [Docker File - SpringBoot - LocketAPIGateway](#backend-springboot---locketapigateway-pipeline-docker)
        - [Jenkins File - SpringBoot - LocketDiscovery](#backend-springboot---locketdiscovery-pipeline)
        - [Docker File - SpringBoot - LocketDiscovery](#backend-springboot---locketdiscovery-pipeline-docker)
        - [Jenkins File - SpringBoot - LocketElasticSearch](#backend-springboot---locketelasticsearch-pipeline)
        - [Docker File - SpringBoot - LocketElasticSearch](#backend-springboot---locketelasticsearch-pipeline-docker)
        - [Jenkins File - SpringBoot - LocketPaymentService](#backend-springboot---locketpaymentservice-pipeline)
        - [Docker File - SpringBoot - LocketPaymentService](#backend-springboot---locketpaymentservice-pipeline-docker)
        - [Jenkins File - SpringBoot - LocketUserService](#backend-springboot---locketuserservice-pipeline)
        - [Docker File - SpringBoot - LocketUserService](#backend-springboot---locketuserservice-pipeline-docker)
        - [Jenkins File - Dispatcher 자동 감지 배포](#backend-springboot---dispatcher-pipeline)
5. [Docker Swarm Stack 및 Monitoring 구성](#monitoring)
    - [docker-stack-monitoring.yml](#docker-stack-monitoringyml)
    - [docker-stack-spring-locket.yml](#docker-stack-spring-locketyml)
    - [prometheus.yml](#prometheusyml)
6. [Docker Compose 설정](#docker-compose)
    - [ElasticSearch 구성](#docker-compose-elasticsearchyml)
    - [Kafka 구성](#docker-compose-kafkayml)
7. [외부 서비스 API](#외부-서비스-api)

## 개발환경
```
[Android]
agp = "8.5.0"
kotlin = "1.9.22"
coreKtx = "1.15.0"
junit = "4.13.2"
junitVersion = "1.2.1"
espressoCore = "3.6.1"
appcompat = "1.7.0"
material = "1.12.0"
activity = "1.10.1"
constraintlayout = "2.2.1"
runtimeAndroid = "1.7.8"
firebaseMessagingKtx = "24.1.1"
recyclerview = "1.1.0"
firebaseCrashlyticsPlugin = "2.9.9"

[Backend(SpringBoot)]
Java = jdk17
Spring Boot = 3.3.3
IntelliJ = 2024.3.1.1

[Backend(FastApi)]
FastAPI = 0.111.0
Python = 3.9.13
Uvicorn = 0.24.0
VSCode = 1.99.1

[Database]
PostgreSQL = 16.8.0
Elastic Search = 8.11.0
Redis = 7.4.2

[Infra]
Ubuntu = 22.04.5 LTS
jenkins = 2.501
Java = 17.0.13
nginx = 1.18.0
Docker = 26.1.3
```

## EC2 포트 번호

| Skill | Port1  | Port2 | Port3 |
| --- |--------|-------|-------|
| Jenkins | 28080  |
| Nginx | 80/443 |
| Redis | 6379   |
| Kafka | 19092  | 19093 | 19094 |
| Kafdrop | 9500   |
| Elastic Search | 9200   |
| Kibana | 5601   |
| Postgre | 5432   | 5433  |
| Eureka | 8761   |
| LocketGateway | 8080   |
| LocketPaymentService | 8081   |
| LocketUserService | 8082   |
| LocketElasticSearch | 8083   |
| Portainer | 9000   |
| Prometheus | 9090   |
| Grafana | 3000   |
| cAdvisor | 8084   |
| Node Exporter | 9100   |



## 환경변수
### LocketApiGateway application.yml
```
spring:
  application:
    name: locket-api-gateway
  profiles:
    active: dev
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin
        - AddResponseHeader=Access-Control-Allow-Headers, Content-Type, Authorization, X-User-Id, X-Auth-UserId
        - PreserveHostHeader
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
            exposedHeaders: "Authorization, Content-Disposition, X-User-Id, X-Auth-UserId"
      routes:
        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payment/**
          filters:
            - RewritePath=/api/payment/(?<segment>.*), /${segment}
            - PreserveHostHeader
            - name: JwtGatewayFilter
              args:
                enabled: true

        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
          filters:
            - RewritePath=/api/users/(?<segment>.*), /${segment}
            - PreserveHostHeader
            - name: JwtGatewayFilter
              args:
                enabled: true

        - id: elasticsearch-service
          uri: lb://ELASTICSEARCH-SERVICE
          predicates:
            - Path=/api/elasticsearch/**
          filters:
            - RewritePath=/api/elasticsearch/(?<segment>.*), /${segment}
            - PreserveHostHeader

        - id: locket-ai-service
          uri: http://localhost:8500
          predicates:
            - Path=/api/ai/**
          filters:
            - StripPrefix=2
            - RewritePath=/api/ai/(?<segment>.*), /${segment}
            - PreserveHostHeader

        - id: payment-service-docs
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payment/v3/api-docs
          filters:
            - RewritePath=/api/payment/v3/api-docs, /payment-docs

        - id: user-service-docs
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/v3/api-docs
          filters:
            - RewritePath=/api/users/v3/api-docs, /users-docs

        - id: elasticsearch-service-docs
          uri: lb://ELASTICSEARCH-SERVICE
          predicates:
            - Path=/api/elasticsearch/v3/api-docs
          filters:
            - RewritePath=/api/elasticsearch/v3/api-docs, /elasticsearch-docs

        - id: locket-ai-service-docs
          uri: http://localhost:8500
          predicates:
            - Path=/api/ai/openapi.json
          filters:
            - RewritePath=/api/ai/openapi.json, /openapi.json

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

jwt:
  secret: [JWT SECRET]
  access-exp: [Access Token Expiration Time in ms]
  refresh-exp: [Refresh Token Expiration Time in ms]

springdoc:
  api-docs:
    enabled: true
    groups:
      enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
```
### LocketApiGateway application-dev.yml
```
spring:
  config:
    activate:
      on-profile: dev

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8080

springdoc:
  swagger-ui:
    urls:
      - name: "Locket Payment Service"
        url: /api/payment/v3/api-docs
      - name: "Locket User Service"
        url: /api/users/v3/api-docs
      - name: "Locket ElasticSearch Service"
        url: /api/elasticsearch/v3/api-docs
      - name: "Locket AI Service"
        url: /api/ai/openapi.json

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

```

### LocketApiGateway application-prod.yml
```
spring:
  config:
    activate:
      on-profile: prod
  application:
    name: locket-api-gateway
  web:
    resources:
      add-mappings: true
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
            exposedHeaders: "Authorization, Content-Disposition"
      routes:
        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payment/**
          filters:
            - RewritePath=/api/payment/(?<segment>/?.*), /${segment}
            - name: JwtGatewayFilter
              args:
                enabled: true
          order: 10

        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**
          filters:
            - RewritePath=/api/users/(?<segment>.*), /${segment}
            - PreserveHostHeader
            - name: JwtGatewayFilter
              args:
                enabled: true
          order: 10

        - id: elasticsearch-service
          uri: lb://ELASTICSEARCH-SERVICE
          predicates:
            - Path=/api/elasticsearch/**
          filters:
            - RewritePath=/api/elasticsearch/(?<segment>/?.*), /${segment}
            - RemoveRequestHeader=Host
          order: 10

        - id: locket-ai-service
          uri: lb://LOCKET-AI-SERVICE
          predicates:
            - Path=/api/ai/**
          filters:
            - RewritePath=/api/ai/(?<segment>/?.*), /api/ai/${segment}
          order: 10

        - id: payment-service-docs
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payment/v3/api-docs/**
          filters:
            - RewritePath=/api/payment/v3/api-docs(?<remaining>/?.*), /payment-docs${remaining}
          order: 1

        - id: user-service-docs
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/v3/api-docs/**
          filters:
            - RewritePath=/api/users/v3/api-docs(?<remaining>/?.*), /users-docs${remaining}
          order: 1

        - id: elasticsearch-service-docs
          uri: lb://ELASTICSEARCH-SERVICE
          predicates:
            - Path=/api/elasticsearch/v3/api-docs/**
          filters:
            - RewritePath=/api/elasticsearch/v3/api-docs(?<remaining>/?.*), /v3/api-docs${remaining}
            - RemoveRequestHeader=Host
          order: 1

        - id: elasticsearch-service-docs-config
          uri: lb://ELASTICSEARCH-SERVICE
          predicates:
            - Path=/api/elasticsearch/v3/api-docs/swagger-config
          filters:
            - RewritePath=/api/elasticsearch/v3/api-docs/swagger-config, /v3/api-docs/swagger-config
            - RemoveRequestHeader=Host
          order: 1

        - id: locket-ai-service-docs
          uri: lb://LOCKET-AI-SERVICE
          predicates:
            - Path=/api/ai/openapi.json
          filters:
            - RewritePath=/api/ai/openapi.json(?<remaining>/?.*), /openapi.json${remaining}
            - RemoveRequestHeader=Host
          order: 1

eureka:
  instance:
    instance-id: api-gateway:${server.port}
    prefer-ip-address: false
    hostname: api-gateway
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://j12d204.p.ssafy.io:8761/eureka

server:
  port: 8080

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    disable-swagger-default-url: true
    urls:
      - name: "1. Locket User Service"
        url: https://j12d204.p.ssafy.io/api/users/v3/api-docs
      - name: "2. Locket Payment Service"
        url: https://j12d204.p.ssafy.io/api/payment/v3/api-docs
      - name: "3. Locket ElasticSearch Service"
        url: https://j12d204.p.ssafy.io/api/elasticsearch/v3/api-docs
      - name: "4. Locket AI Service"
        url: https://j12d204.p.ssafy.io/api/ai/openapi.json

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway, readiness, liveness, info
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

logging:
  level:
    org.springframework.cloud.gateway: TRACE
    reactor.netty.http.client: DEBUG

```

### LocketDiscovery application.yml
```
spring:
  profiles:
    active: dev  # 기본은 dev, 배포 시 prod로 오버라이드

server:
  port: 8761  # Eureka 서버 포트

eureka:
  instance:
    hostname: localhost  # 기본값 (dev용), prod에서는 오버라이드됨
  client:
    registerWithEureka: false  # 자기 자신은 Eureka에 등록하지 않음
    fetchRegistry: false       # 서비스 목록 조회 안 함 (Eureka 서버니까)

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

```

### LocketDiscovery application-dev.yml
```
eureka:
  instance:
    hostname: localhost  # 개발 환경에서는 localhost 기준
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

```

### LocketDiscovery application-prod.yml
```
spring:
  application:
    name: discovery

server:
  port: 8761

eureka:
  instance:
    hostname: j12d204.p.ssafy.io  # ✅ EC2 도메인 기준으로 Eureka가 자신을 인식
    prefer-ip-address: false

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://j12d204.p.ssafy.io:8761/eureka

  server:
    enable-self-preservation: false

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, gateway, readiness, liveness
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

```

### LocketPaymentService application.yml
```
server:
  port: 8081

spring:
  application:
    name: PAYMENT-SERVICE
  profiles:
    active: dev
    include: key  # application-key.yml 포함 (DB, Kafka 정보)
  datasource:
    hikari:
      maximum-pool-size: 15
      minimum-idle: 30
      idle-timeout: 300000
      max-lifetime: 1800000
      connection-timeout: 3000
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: ${KAFKA.BOOTSTRAP-SERVERS}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: payment-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
    listener:
      ack-mode: manual
  data:
    redis:
      host: j12d204.p.ssafy.io
      port: 6379
      password: [REDIS PASSWORD]

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /payment-docs/swagger-ui.html
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

jwt:
  secret: [JWT SECRET]
  access-exp: [Access Token Expiration Time in ms]
  refresh-exp: [Refresh Token Expiration Time in ms]

```

### LocketPaymentService application-dev.yml
```
spring:
  datasource:
    url: [DB URL]
    username: [DB USERNAME]
    password: [DB PASSWORD]
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

swagger:
  server-url: http://localhost:8080/api/payment

feign:
  client:
    elasticsearch-service:
      url: http://localhost:8083

logging:
  level:
    com.locket: INFO

```

### LocketPaymentService application-prod.yml
```
server:
  port: 8081
  tomcat:
    relaxed-host-chars: "_"

DB:
  URL_PAYMENT: [DB URL]
  USERNAME: [DB USERNAME]
  PASSWORD: [DB PASSWORD]

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: [DB URL]
    username: [DB USERNAME]
    password: [DB PASSWORD]
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  data:
    redis:
      host: j12d204.p.ssafy.io
      port: 6379
      password: [REDIS PASSWORD]

eureka:
  instance:
    instance-id: payment-service:${server.port}
    prefer-ip-address: false
    hostname: payment-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://j12d204.p.ssafy.io:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, gateway, readiness, liveness
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /payment-docs
  swagger-ui:
    path: /payment-docs/swagger-ui.html
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

swagger:
  server-url: https://j12d204.p.ssafy.io/api/payment

feign:
  client:
    elasticsearch-service:
      url: http://172.26.5.222:8083

logging:
  level:
    root: WARN
    com.locket: WARN

```

### LocketUserService application.yml
```
server:
  port: 8082
  tomcat:
    relaxed-host-chars: "_"

spring:
  application:
    name: USER-SERVICE
  profiles:
    active: dev
    include: key

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  kafka:
    bootstrap-servers: [KAFKA SERVER]
    consumer:
      group-id: [USER GROUP ID]
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"
    listener:
      ack-mode: manual_immediate

  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 5000
            read-timeout: 5000

  data:
    redis:
      host: j12d204.p.ssafy.io
      port: 6379
      password: [REDIS PASSWORD]

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /users-docs/swagger-ui.html
    display-request-duration: true
    filter: true
    use-root-path: true
  model-converters:
    pageable-converter:
      enabled: true
  models:
    schema-format: JSON_SCHEMA
    show-actuator: false
    override-with-generic-response: false
    use-fqn: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

jwt:
  secret: [JWT SECRET]
  access-exp: [Access Token Expiration Time in ms]
  refresh-exp: [Refresh Token Expiration Time in ms]

receipt:
  service:
    url: https://j12d204.p.ssafy.io

app:
  test-auth:
    enabled: true

```

### LocketUserService application-dev.yml
```
spring:
  datasource:
    url: [DB URL]
    username: [DB USERNAME]
    password: [DB PASSWORD]
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

swagger:
  server-url: http://localhost:8080/api/users

feign:
  client:
    elasticsearch-service:
      url: http://localhost:8083

logging:
  level:
    com.locket: INFO

```

### LocketUserService application-prod.yml
```
server:
  port: 8082
  tomcat:
    relaxed-host-chars: "_"

DB:
  URL_USER: [DB URL]
  USERNAME: [DB USERNAME]
  PASSWORD: [DB PASSWORD]

spring:
  config:
    activate:
      on-profile: prod

  kafka:
    consumer:
      group-id: user-service-group-prod

  datasource:
    url: [DB URL]
    username: [DB USERNAME]
    password: [DB PASSWORD]

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  data:
    redis:
      host: j12d204.p.ssafy.io
      port: 6379
      password: [REDIS PASSWORD]

  locket:
    receipt:
      service:
        url: https://j12d204.p.ssafy.io:8500

eureka:
  instance:
    instance-id: user-service:${server.port}
    prefer-ip-address: false
    hostname: user-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://j12d204.p.ssafy.io:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, gateway, readiness, liveness
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /users-docs
  swagger-ui:
    path: /users-docs/swagger-ui.html
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

swagger:
  server-url: https://j12d204.p.ssafy.io/api/users

PERPLEXITY:
  API_KEY: [PERPLEXITY API KEY]

feign:
  client:
    elasticsearch-service:
      url: http://172.26.5.222:8083

logging:
  level:
    root: WARN
    com.locket: WARN

```

### LocketUserService firebase-key.json
```
{
  "type": "service_account",
  "project_id": "locket-a4505",
  "private_key_id": "[MASKED]",
  "private_key": "[MASKED]",
  "client_email": "firebase-adminsdk-fbsvc@locket-a4505.iam.gserviceaccount.com",
  "client_id": "[MASKED]",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "https://www.googleapis.com/robot/v1/metadata/x509/firebase-adminsdk-fbsvc%40locket-a4505.iam.gserviceaccount.com",
  "universe_domain": "googleapis.com"
}

```

### LocketElasticSearch application.yml
```
server:
  port: 8083

spring:
  application:
    name: elasticsearch-service
  profiles:
    active: dev
    include: key

  kafka:
    bootstrap-servers: [KAFKA BOOTSTRAP SERVERS]
    consumer:
      group-id: [ELASTICSEARCH GROUP ID]
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"
    listener:
      ack-mode: manual_immediate

management:
  endpoints:
    web:
      exposure:
        include: prometheus, health, gateway
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /elasticsearch-docs/swagger-ui.html
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

```

### LocketElasticSearch application-dev.yml
```
spring:
  elasticsearch:
    uris: [ELASTICSEARCH DEV URIS]
    username: [ELASTICSEARCH DEV USERNAME]
    password: [ELASTICSEARCH DEV PASSWORD]

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

swagger:
  server-url: http://localhost:8080/api/elasticsearch

```

### LocketElasticSearch application-prod.yml
```
server:
  port: 8083
  tomcat:
    relaxed-host-chars: "_"
    reject-illegal-header: false

ELASTICSEARCH:
  URIS: [ELASTICSEARCH PROD URIS]
  USERNAME: [ELASTICSEARCH PROD USERNAME]
  PASSWORD: [ELASTICSEARCH PROD PASSWORD]

spring:
  application:
    name: elasticsearch-service
  config:
    activate:
      on-profile: prod

  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns: "*"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
            exposedHeaders: "Authorization, Content-Disposition"

  kafka:
    consumer:
      group-id: elastic-service-group-prod

  elasticsearch:
    uris: [ELASTICSEARCH PROD URIS]
    username: [ELASTICSEARCH PROD USERNAME]
    password: [ELASTICSEARCH PROD PASSWORD]

  locket:
    receipt:
      service:
        url: https://j12d204.p.ssafy.io:8500

eureka:
  instance:
    instance-id: elasticsearch-service:${server.port}
    prefer-ip-address: false
    hostname: elasticsearch-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://j12d204.p.ssafy.io:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, gateway, readiness, liveness
  endpoint:
    prometheus:
      enabled: true
    gateway:
      enabled: true
    health:
      show-details: always
      probes:
        enabled: true
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    path: /v3/api-docs/swagger-ui.html

swagger:
  server-url: https://j12d204.p.ssafy.io/api/elasticsearch

locket:
  ai:
    service:
      url: http://j12d204.p.ssafy.io:8500

```

### LocketAIService .env
```
CLOVA_OCR_URL=[CLOVA OCR URL]
CLOVA_OCR_SECRET=[CLOVA OCR SECRET]

OPENAI_API_KEY=[OPENAI API KEY]

ENV=[Locket AI Service Profile]
PORT=[Locket AI Service Port]
SERVICE_NAME=LOCKET-AI-SERVICE
SERVICE_HOST=[Locket AI Service HOST]
EUREKA_SERVER=[EUREKA SERVER]
```

### LocketAIService requirements.txt
```
annotated-types==0.7.0
anyio==3.7.1
certifi==2023.11.17
charset-normalizer==3.4.1
click==8.1.8
colorama==0.4.6
distro==1.9.0
fastapi==0.111.0
fasttext-wheel==0.9.2
h11==0.14.0
httpcore==1.0.7
httpx==0.28.1
idna==3.10
jiter==0.9.0
joblib==1.3.2
numpy==1.24.3
openai==1.69.0
pdf2image==1.16.3
pillow==11.1.0
pybind11==2.13.6
pydantic==2.6.4
pydantic-settings==2.2.1
PyPDF2==3.0.1
python-decouple==3.8
python-dotenv==1.0.0
python-multipart
requests==2.31.0
scikit-learn==1.3.0
scipy==1.15.2
sniffio==1.3.1
starlette==0.37.2
threadpoolctl==3.6.0
tqdm==4.67.1
typing_extensions==4.12.2
urllib3==2.3.0
uvicorn==0.24.0
py_eureka_client==0.11.13
prometheus_client==0.20.0
```
### Monitoring
#### docker-stack-monitoring.yml
```
version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    volumes:
      - /home/ubuntu/S12P21D204/backend/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    networks:
      - monitoring
      - spring-locket

  grafana:
    image: grafana/grafana:latest
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
    ports:
      - "3000:3000"
    networks:
      - monitoring
      - spring-locket

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    ports:
      - "8084:8080"
    networks:
      - monitoring
      - spring-locket

  node-exporter:
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
    networks:
      - monitoring
      - spring-locket

networks:
  monitoring:
    external: true
  spring-locket:
    external: true

```

#### docker-stack-spring-locket.yml
```
version: '3.8'

services:
  discovery:
    image: maru1203/spring-locket-discovery:latest
    ports:
      - "8761:8761"
    networks:
      - spring-locket
      - monitoring

  user-service:
    image: maru1203/spring-locket-user-service:latest
    ports:
      - "8082:8082"
    networks:
      - spring-locket
      - monitoring
    depends_on:
      - discovery

  payment-service:
    image: maru1203/spring-locket-payment-service:latest
    ports:
      - "8081:8081"
    networks:
      - spring-locket
      - monitoring

  api-gateway:
    image: maru1203/spring-locket-api-gateway:latest
    ports:
      - "8080:8080"
    networks:
      - spring-locket
      - monitoring

networks:
  spring-locket:
    external: true
  monitoring:
    external: true

```

#### prometheus.yml
```
global:
  scrape_interval: 15s  # 메트릭 수집 주기

scrape_configs:
  - job_name: 'spring-actuator'  # Spring Boot Actuator 엔드포인트 수집
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets:
          - '172.26.5.222:8761'                 # 🔍 LocketDiscovery
          - '172.26.5.222:8082'                 # 👤 LocketUserService
          - '172.26.5.222:8081'                 # 💰 LocketPaymentService
          - 'spring-locket_api-gateway:8080'    # 🌐 LocketApiGateway
          - 'spring-locket-elasticsearch:8083'  # 🔎 LocketElasticSearchService

  - job_name: 'cadvisor'  # 컨테이너 리소스 모니터링 (cAdvisor)
    static_configs:
      - targets:
          - 'monitoring_cadvisor:8080'

  - job_name: 'node-exporter'  # 호스트 리소스 모니터링 (Node Exporter)
    static_configs:
      - targets:
          - 'monitoring_node-exporter:9100'

  - job_name: 'ai-service'  # FastAPI 기반 AI 서비스 (Prometheus 직접 노출 방식)
    metrics_path: '/metrics'
    static_configs:
      - targets:
          - 'spring-locket-ai-service:8500'

```

### Docker Compose 
#### docker-compose-elasticsearch.yml
```
version: "3.9"

networks:
  elk-network:
    driver: bridge

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ELASTIC_USERNAME=[ELASTIC_USERNAME]
      - ELASTIC_PASSWORD=[ELASTIC_PASSWORD]
    ports:
      - "9200:9200"
      - "9300:9300"
    volumes:
      - es_data:/usr/share/elasticsearch/data
    networks:
      - elk-network
    restart: unless-stopped

  kibana:
    image: docker.elastic.co/kibana/kibana:8.11.0
    container_name: kibana
    environment:
      - ELASTICSEARCH_HOSTS=[ELASTICSEARCH_HOSTS]
    ports:
      - "5601:5601"
    depends_on:
      - elasticsearch
    networks:
      - elk-network
    restart: unless-stopped

volumes:
  es_data:
    driver: local

```

#### docker-compose-kafka.yml
```
version: "3.9"

networks:
  kafka-cluster:
    driver: bridge

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      KAFKA_OPTS: "-Dzookeeper.4lw.commands.whitelist=*"
    ports:
      - "2181:2181"
    restart: unless-stopped
    networks:
      - kafka-cluster

  kafka-1:
    image: confluentinc/cp-kafka:latest
    container_name: kafka-1
    depends_on:
      - zookeeper
    ports:
      - "19092:19092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:9093,EXTERNAL://0.0.0.0:19092
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-1:9093,EXTERNAL://[PUBLIC HOST]:19092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 2
    restart: unless-stopped
    networks:
      - kafka-cluster

  kafka-2:
    image: confluentinc/cp-kafka:latest
    container_name: kafka-2
    depends_on:
      - zookeeper
    ports:
      - "19093:19093"
    environment:
      KAFKA_BROKER_ID: 2
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:9094,EXTERNAL://0.0.0.0:19093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-2:9094,EXTERNAL://[PUBLIC HOST]:19093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 2
    restart: unless-stopped
    networks:
      - kafka-cluster

  kafka-3:
    image: confluentinc/cp-kafka:latest
    container_name: kafka-3
    depends_on:
      - zookeeper
    ports:
      - "19094:19094"
    environment:
      KAFKA_BROKER_ID: 3
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENERS: INTERNAL://0.0.0.0:9095,EXTERNAL://0.0.0.0:19094
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka-3:9095,EXTERNAL://[PUBLIC HOST]:19094
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 2
    restart: unless-stopped
    networks:
      - kafka-cluster

  kafdrop:
    image: obsidiandynamics/kafdrop:latest
    container_name: kafdrop
    ports:
      - "9000:9000"
    environment:
      KAFKA_BROKERCONNECT: [PUBLIC HOST]:19092,[PUBLIC HOST]:19093,[PUBLIC HOST]:19094
      JVM_OPTS: "-Xms256M -Xmx512M"
    networks:
      - kafka-cluster

```

## CI/CD 구축
### Jenkins 설치 플러그인
1. Pipeline
2. Pipeline: Stage View
3. Pipeline: GitHub
4. Git Plugin
5. Git Parameter Plugin
6. GitLab Plugin
7. Docker Pipeline
8. Docker Commons
9. Credentials Binding Plugin
10. Gradle Plugin
11. Matrix Project Plugin

### Jenkins Credentials
<img src="https://velog.velcdn.com/images/aoreo0017/post/ae935fd3-cb08-49fd-9683-8a9e1ad72967/image.png"/>

### Pipeline
#### Backend (FastAPI - LocketAIService) Pipeline
```
pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'spring-locket-ai-service'
        DOCKER_PORT = 8500

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')

        JAVA_HOME = '/opt/java/openjdk'
        PATH = "${JAVA_HOME}/bin:${env.PATH}"
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building the Docker image...'
                dir('backend/LocketAIService') {
                    sh 'docker build -t ${DOCKER_IMAGE}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2 (UPDATE)...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    ),
                    string(credentialsId: 'CLOVA_OCR_URL', variable: 'CLOVA_OCR_URL'),
                    string(credentialsId: 'CLOVA_OCR_SECRET', variable: 'CLOVA_OCR_SECRET'),
                    string(credentialsId: 'OPENAI_API_KEY', variable: 'OPENAI_API_KEY')
                ]) {
                    sh """
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP \
                        "docker service update \
                            --image ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest \
                            --env-add PORT=${DOCKER_PORT} \
                            --env-add EUREKA_SERVER=http://j12d204.p.ssafy.io:8761/eureka \
                            --env-add SERVICE_NAME=LOCKET-AI-SERVICE \
                            --env-add CLOVA_OCR_URL=${CLOVA_OCR_URL} \
                            --env-add CLOVA_OCR_SECRET=${CLOVA_OCR_SECRET} \
                            --env-add OPENAI_API_KEY=${OPENAI_API_KEY} \
                            ${DOCKER_IMAGE}"
                    """
                }
            }
        }
    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ LocketAIService 최초 배포 성공 (create only) !'
        }
        failure {
            echo '❌ 최초 배포 실패. 로그 확인 필요.'
        }
    }
}
``` 

#### Backend (FastAPI - LocketAIService) Pipeline Docker
```
# ---- Base image ----
FROM python:3.10-slim

# ---- Environment variables ----
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PORT=8500 \
    EUREKA_SERVER=http://eureka-server:8761/eureka \
    SERVICE_NAME=LOCKET-AI-SERVICE

# ---- Working directory ----
WORKDIR /app

# ---- Install dependencies ----
COPY requirements.txt .
RUN apt-get update && \
    apt-get install -y curl iputils-ping procps poppler-utils && \
    pip install --upgrade pip && \
    pip install -r requirements.txt && \
    apt-get clean


# ---- Copy application code ----
COPY . .

# ---- Expose port ----
EXPOSE ${PORT}

# ---- HEALTHCHECK ----
#HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
#  CMD curl -f http://localhost:8500/health || exit 1

# ---- Start application ----
#CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8500"]
CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port $PORT"]
```

#### Backend (SpringBoot - LocketAPIGateway) Pipeline
```
pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'spring-locket_api-gateway'
        DOCKER_PORT = 8080

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')
        EUREKA_SERVER = credentials('EUREKA_SERVER')

        JAVA_HOME = '/opt/java/openjdk'
        GRADLE_HOME = '/opt/gradle/gradle-8.7'
        PATH = "${JAVA_HOME}/bin:${GRADLE_HOME}/bin:${env.PATH}"
    }

    tools {
        jdk 'JDK17'
        gradle 'Gradle 8.7'
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Build Application') {
            steps {
                echo '🔨 Building the API Gateway...'
                dir('backend/LocketApiGateway') {
                    sh 'gradle clean build -x test'
                    sh 'ls -al $(pwd)/build/libs'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building the API Gateway Docker image...'
                dir('backend/LocketApiGateway') {
                    sh 'cp build/libs/LocketApiGateway-0.0.1-SNAPSHOT.jar .'
                    sh 'docker build -t ${DOCKER_IMAGE}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    string(credentialsId: 'EUREKA_SERVER', variable: 'EUREKA_SERVER'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    )
                ]) {
                    sh '''
                        echo "🚀 Updating Docker Swarm service..."
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP \
                            "docker service update \
                                --env-rm SPRING_PROFILES_ACTIVE \
                                --env-add SPRING_PROFILES_ACTIVE=prod \
                                --env-add EUREKA_SERVER=$EUREKA_SERVER \
                                --image $DOCKERHUB_USERNAME/${DOCKER_IMAGE}:latest \
                                ${DOCKER_IMAGE}"
                    '''
                }
            }
        }

    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed.'
        }
    }
}
```

#### Backend (SpringBoot - LocketAPIGateway) Pipeline Docker
```
# 1. Java 베이스 이미지 사용 (JDK 17 Slim 버전)
FROM openjdk:17-jdk-slim

# 🔧 curl 설치 (healthcheck용)
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 2. 작업 디렉토리 생성 및 설정
WORKDIR /app

# 3. 빌드된 JAR 파일 복사
COPY build/libs/LocketApiGateway-0.0.1-SNAPSHOT.jar app.jar

# 4. 컨테이너에서 노출할 포트 (게이트웨이의 경우 보통 8080 사용)
EXPOSE 8080

# 5. 헬스체크 명령 추가 (actuator를 사용하는 경우)
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD curl -f http://localhost:8080/actuator/health/liveness || exit 1

# 6. 애플리케이션 실행 명령어
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

#### Backend (SpringBoot - LocketDiscovery) Pipeline
```
pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'spring-locket_discovery'
        DOCKER_PORT = 8761

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')

        JAVA_HOME = '/opt/java/openjdk'
        GRADLE_HOME = '/opt/gradle/gradle-8.7'
        PATH = "${JAVA_HOME}/bin:${GRADLE_HOME}/bin:${env.PATH}"
    }

    tools {
        jdk 'JDK17'
        gradle 'Gradle 8.7'
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Build Application') {
            steps {
                echo '🔨 Building the Discovery Service...'
                dir('backend/LocketDiscovery') {
                    sh 'gradle clean build -x test'
                    sh 'ls -al $(pwd)/build/libs'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building the Discovery Service Docker image...'
                dir('backend/LocketDiscovery') {
                    sh 'cp build/libs/LocketDiscovery-0.0.1-SNAPSHOT.jar .'
                    sh 'docker build -t ${DOCKER_IMAGE}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    )
                ]) {
                    sh '''
                        echo "🚀 Updating Docker Swarm service..."
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP \
                            "docker service update \
                                --env-rm SPRING_PROFILES_ACTIVE \
                                --env-add SPRING_PROFILES_ACTIVE=prod \
                                --env-add DOCKER_PORT=$DOCKER_PORT \
                                --image $DOCKERHUB_USERNAME/${DOCKER_IMAGE}:latest \
                                ${DOCKER_IMAGE}"
                    '''
                }
            }
        }

    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed.'
        }
    }
}
```

#### Backend (SpringBoot - LocketDiscovery) Pipeline Docker
```
# 1. Java 베이스 이미지 사용 (JDK 17 Slim 버전)
FROM openjdk:17-jdk-slim

# 🔧 curl 설치 (healthcheck용)
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 2. 작업 디렉토리 생성 및 설정
WORKDIR /app

# 3. 빌드된 JAR 파일 복사
COPY build/libs/LocketDiscovery-0.0.1-SNAPSHOT.jar app.jar

# 4. 컨테이너에서 노출할 포트 (Eureka 서버 기본 포트: 8761)
EXPOSE 8761

# 5. 헬스체크 명령 추가 (actuator를 사용하는 경우)
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD curl -f http://localhost:8761/actuator/health || exit 1

# 6. 애플리케이션 실행 명령어
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]s
```

#### Backend (SpringBoot - LocketElasticSearch) Pipeline
```
pipeline {
    agent any

    environment {
        DOCKER_IMAGE_NAME = 'spring-locket-elasticsearch'
        DOCKER_SERVICE_NAME = 'spring-locket-elasticsearch'
        DOCKER_PORT = 8083

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')

        ELASTICSEARCH_URIS = credentials('ELASTICSEARCH_URIS')
        ELASTICSEARCH_USERNAME = credentials('ELASTICSEARCH_USERNAME')
        ELASTICSEARCH_PASSWORD = credentials('ELASTICSEARCH_PASSWORD')
        KAFKA_BOOTSTRAP_SERVERS = credentials('KAFKA_BOOTSTRAP_SERVERS')

        JAVA_HOME = '/opt/java/openjdk'
        GRADLE_HOME = '/opt/gradle/gradle-8.7'
        PATH = "${JAVA_HOME}/bin:${GRADLE_HOME}/bin:${env.PATH}"
    }

    tools {
        jdk 'JDK17'
        gradle 'Gradle 8.7'
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Build Application') {
            steps {
                echo '🔨 Building LocketElasticSearch...'
                dir('backend/LocketElasticSearch') {
                    sh 'gradle clean build -x test'
                    sh 'ls -al $(pwd)/build/libs'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building Docker image...'
                dir('backend/LocketElasticSearch') {
                    sh 'cp build/libs/LocketElasticSearch-0.0.1-SNAPSHOT.jar .'
                    sh 'docker build -t ${DOCKER_IMAGE_NAME}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE_NAME}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE_NAME}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE_NAME}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    ),
                    string(credentialsId: 'ELASTICSEARCH_URIS', variable: 'ELASTICSEARCH_URIS'),
                    string(credentialsId: 'ELASTICSEARCH_USERNAME', variable: 'ELASTICSEARCH_USERNAME'),
                    string(credentialsId: 'ELASTICSEARCH_PASSWORD', variable: 'ELASTICSEARCH_PASSWORD'),
                    string(credentialsId: 'KAFKA_BOOTSTRAP_SERVERS', variable: 'KAFKA_BOOTSTRAP_SERVERS')
                ]) {
                    sh '''
                        echo "🔁 Updating Docker service..."
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP "
                            docker service update \
                                --env-rm SPRING_PROFILES_ACTIVE \
                                --env-add SPRING_PROFILES_ACTIVE=prod \
                                --env-add ELASTICSEARCH_URIS=$ELASTICSEARCH_URIS \
                                --env-add ELASTICSEARCH_USERNAME=$ELASTICSEARCH_USERNAME \
                                --env-add ELASTICSEARCH_PASSWORD=$ELASTICSEARCH_PASSWORD \
                                --env-add KAFKA_BOOTSTRAP_SERVERS=$KAFKA_BOOTSTRAP_SERVERS \
                                --env-add LOCKET_RECEIPT_SERVICE_URL=https://j12d204.p.ssafy.io:8500 \
                                --image $DOCKERHUB_USERNAME/${DOCKER_IMAGE_NAME}:latest \
                                ${DOCKER_SERVICE_NAME}
                        "
                    '''
                }
            }
        }
    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed.'
        }
    }
}
```

#### Backend (SpringBoot - LocketElasticSearch) Pipeline Docker
```
# 1. Java 베이스 이미지 사용 (JDK 17 Slim 버전)
FROM openjdk:17-jdk-slim

# 🔧 curl 설치 (healthcheck용)
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 2. 작업 디렉토리 생성 및 설정
WORKDIR /app

# 3. 빌드된 JAR 파일 복사
COPY build/libs/LocketElasticSearch-0.0.1-SNAPSHOT.jar app.jar

# 4. 컨테이너에서 노출할 포트
EXPOSE 8083

# 5. 헬스체크 명령 추가 (actuator를 사용하는 경우)
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD curl -f http://localhost:8083/actuator/health || exit 1

# 6. 애플리케이션 실행 명령어
#ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
ENTRYPOINT ["java", "-Dserver.tomcat.relaxed-host-chars=_", "-Dserver.tomcat.reject-illegal-header=false", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

#### Backend (SpringBoot - LocketPaymentService) Pipeline
```
pipeline {s
    agent any

    environment {
        DOCKER_IMAGE = 'spring-locket_payment-service'
        DOCKER_PORT = 8081

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')

        DB_URL_PAYMENT = credentials('DB_URL_PAYMENT')
        DB_USERNAME = credentials('DB_USERNAME')
        DB_PASSWORD = credentials('DB_PASSWORD')
        KAFKA_BOOTSTRAP_SERVERS = credentials('KAFKA_BOOTSTRAP_SERVERS')

        REDIS_PASSWORD = credentials('REDIS_PASSWORD')

        JAVA_HOME = '/opt/java/openjdk'
        GRADLE_HOME = '/opt/gradle/gradle-8.7'
        PATH = "${JAVA_HOME}/bin:${GRADLE_HOME}/bin:${env.PATH}"
    }

    tools {
        jdk 'JDK17'
        gradle 'Gradle 8.7'
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Build Application') {
            steps {
                echo '🔨 Building the application with Gradle Wrapper...'
                dir('backend/LocketPaymentService') {
                    sh 'gradle clean build -x test'
                    sh 'ls -al $(pwd)/build/libs'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building the Docker image...'
                dir('backend/LocketPaymentService') {
                    sh 'cp build/libs/LocketPaymentService-0.0.1-SNAPSHOT.jar .'
                    sh 'docker build -t ${DOCKER_IMAGE}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    ),
                    string(credentialsId: 'REDIS_PASSWORD', variable: 'REDIS_PASSWORD'),
                    string(credentialsId: 'DB_URL_PAYMENT', variable: 'DB_URL_PAYMENT'),
                    string(credentialsId: 'DB_USERNAME', variable: 'DB_USERNAME'),
                    string(credentialsId: 'DB_PASSWORD', variable: 'DB_PASSWORD'),
                    string(credentialsId: 'KAFKA_BOOTSTRAP_SERVERS', variable: 'KAFKA_BOOTSTRAP_SERVERS')
                ]) {
                    sh '''
                        echo "🚀 Updating Docker Swarm service..."
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP \
                            "docker service update \
                                --env-rm SPRING_PROFILES_ACTIVE \
                                --env-add SPRING_PROFILES_ACTIVE=prod \
                                --env-add REDIS_PASSWORD=$REDIS_PASSWORD \
                                --env-add DB_URL_PAYMENT=$DB_URL_PAYMENT \
                                --env-add DB_USERNAME=$DB_USERNAME \
                                --env-add DB_PASSWORD=$DB_PASSWORD \
                                --env-add KAFKA_BOOTSTRAP_SERVERS=$KAFKA_BOOTSTRAP_SERVERS \
                                --image $DOCKERHUB_USERNAME/${DOCKER_IMAGE}:latest \
                                ${DOCKER_IMAGE}"
                    '''
                }
            }
        }

    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed.'
        }
    }
}
```

#### Backend (SpringBoot - LocketPaymentService) Pipeline Docker
```
# 1. Java 베이스 이미지 사용 (JDK 17 Slim 버전)
FROM openjdk:17-jdk-slim

# 🔧 healthcheck를 위한 curl 설치
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 2. 작업 디렉토리 생성 및 설정
WORKDIR /app

# 3. 빌드된 JAR 파일 복사
COPY build/libs/LocketPaymentService-0.0.1-SNAPSHOT.jar app.jar

# 4. 컨테이너에서 노출할 포트
EXPOSE 8081

# 5. 헬스체크 명령 추가 (actuator를 사용하는 경우)
HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 \
  CMD curl -f http://localhost:8081/actuator/health || exit 1

# 6. 애플리케이션 실행 명령어
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

#### Backend (SpringBoot - LocketUserService) Pipeline
```
pipeline {
    agent any

    environment {
        DOCKER_IMAGE = 'spring-locket_user-service'
        DOCKER_PORT = 8082

        EC2_USER = credentials('EC2_USER')
        EC2_IP = credentials('EC2_IP')
        SSH_KEY = credentials('SSH_KEY')

        DB_URL_USER = credentials('DB_URL_USER')
        DB_USERNAME = credentials('DB_USERNAME')
        DB_PASSWORD = credentials('DB_PASSWORD')

        KAFKA_BOOTSTRAP_SERVERS = credentials('KAFKA_BOOTSTRAP_SERVERS')

        FIREBASE_KEY = credentials('FIREBASE_KEY_FILE')
        REDIS_PASSWORD = credentials('REDIS_PASSWORD')

        PERPLEXITY_API_KEY = credentials('PERPLEXITY_API_KEY')

        JAVA_HOME = '/opt/java/openjdk'
        GRADLE_HOME = '/opt/gradle/gradle-8.7'
        PATH = "${JAVA_HOME}/bin:${GRADLE_HOME}/bin:${env.PATH}"
    }

    tools {
        jdk 'JDK17'
        gradle 'Gradle 8.7'
    }

    stages {
        stage('Clone Repository') {
            steps {
                echo '🔄 Cloning the repository...'
                git branch: 'backend',
                    url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git',
                    credentialsId: 'GitLab-PAT'
            }
        }

        stage('Prepare Firebase Key') {
            steps {
                echo '🔐 Copying Firebase service account key into resources...'
                dir('backend/LocketUserService/src/main/resources') {
                    sh 'cp $FIREBASE_KEY firebase-key.json'
                }
            }
        }

        stage('Build Application') {
            steps {
                echo '🔨 Building the application with Gradle Wrapper...'
                dir('backend/LocketUserService') {
                    sh 'gradle clean build -x test'
                    sh 'ls -al $(pwd)/build/libs'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo '🐳 Building the Docker image...'
                dir('backend/LocketUserService') {
                    sh 'cp build/libs/LocketUserService-0.0.1-SNAPSHOT.jar .'
                    sh 'docker build -t ${DOCKER_IMAGE}:latest .'
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo '📤 Pushing Docker image to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'DockerHub_PAT',
                    usernameVariable: 'DOCKERHUB_USERNAME',
                    passwordVariable: 'DOCKERHUB_TOKEN'
                )]) {
                    sh '''
                        docker login -u ${DOCKERHUB_USERNAME} -p ${DOCKERHUB_TOKEN}
                        docker tag ${DOCKER_IMAGE}:latest ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                        docker push ${DOCKERHUB_USERNAME}/${DOCKER_IMAGE}:latest
                    '''
                }
            }
        }

        stage('Deploy to Docker Swarm') {
            steps {
                echo '🚀 Deploying to Docker Swarm on EC2...'
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'SSH_KEY',
                        keyFileVariable: 'EC2_SSH_KEY_FILE',
                        usernameVariable: 'EC2_USER'
                    ),
                    string(credentialsId: 'EC2_IP', variable: 'EC2_IP'),
                    usernamePassword(
                        credentialsId: 'DockerHub_PAT',
                        usernameVariable: 'DOCKERHUB_USERNAME',
                        passwordVariable: 'DOCKERHUB_TOKEN'
                    ),
                    string(credentialsId: 'REDIS_PASSWORD', variable: 'REDIS_PASSWORD'),
                    string(credentialsId: 'DB_URL_USER', variable: 'DB_URL_USER'),
                    string(credentialsId: 'DB_USERNAME', variable: 'DB_USERNAME'),
                    string(credentialsId: 'DB_PASSWORD', variable: 'DB_PASSWORD'),
                    string(credentialsId: 'KAFKA_BOOTSTRAP_SERVERS', variable: 'KAFKA_BOOTSTRAP_SERVERS'),
                    file(credentialsId: 'FIREBASE_KEY_FILE', variable: 'FIREBASE_KEY')
                ]) {
                    sh '''
                        echo "🚀 Updating Docker Swarm service..."
                        ssh -o StrictHostKeyChecking=no -i $EC2_SSH_KEY_FILE $EC2_USER@$EC2_IP \
                            "docker service update \
                                --env-rm SPRING_PROFILES_ACTIVE \
                                --env-add SPRING_PROFILES_ACTIVE=prod \
                                --env-add REDIS_PASSWORD=$REDIS_PASSWORD \
                                --env-add DB_URL_USER=$DB_URL_USER \
                                --env-add DB_USERNAME=$DB_USERNAME \
                                --env-add DB_PASSWORD=$DB_PASSWORD \
                                --env-add KAFKA_BOOTSTRAP_SERVERS=$KAFKA_BOOTSTRAP_SERVERS \
                                --env-add FIREBASE_KEY=$FIREBASE_KEY \
                                --env-add PERPLEXITY_API_KEY=$PERPLEXITY_API_KEY \
                                --image $DOCKERHUB_USERNAME/${DOCKER_IMAGE}:latest \
                                ${DOCKER_IMAGE}"
                    '''
                }
            }
        }
    }

    post {
        always {
            echo '🧹 Cleaning workspace...'
            cleanWs()
        }
        success {
            echo '✅ Deployment successful!'
        }
        failure {
            echo '❌ Deployment failed.'
        }
    }
}
```

#### Backend (SpringBoot - LocketUserService) Pipeline Docker
```
# 1. Java 베이스 이미지 사용 (JDK 17 Slim 버전)
FROM openjdk:17-jdk-slim

# 🔧 curl 설치 (healthcheck용)
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

# 2. 작업 디렉토리 생성 및 설정
WORKDIR /app

# 3. 빌드된 JAR 파일 복사
COPY build/libs/LocketUserService-0.0.1-SNAPSHOT.jar app.jar

# 4. 컨테이너에서 노출할 포트
EXPOSE 8082

# 5. 헬스체크 명령 추가 (actuator를 사용하는 경우)
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
  CMD curl -f http://localhost:8082/actuator/health || exit 1

# 6. 애플리케이션 실행 명령어
ENTRYPOINT ["java", "-jar", "app.jar", "--spring.profiles.active=prod"]
```

#### Backend (SpringBoot - Dispatcher) Pipeline
```
pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'GitLab-PAT',
                        branch: 'backend',
                        url: 'https://lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git'
            }
        }

        stage('Detect Changed Services') {
            steps {
                withCredentials([usernamePassword(
                        credentialsId: 'GitLab-USER-TOKEN',
                        usernameVariable: 'GIT_USERNAME',
                        passwordVariable: 'GIT_PASSWORD'
                )]) {
                    script {
                        // 1. 클로저 정의: 변경 감지 후 순차 빌드
                        def triggerBuilds = { changes ->
                            if (changes.any { it.startsWith("backend/LocketDiscovery/") }) {
                                echo "🔍 변경 감지됨: backend-discovery"
                                try {
                                    build job: 'backend-discovery'
                                    echo "✅ 빌드 성공: backend-discovery"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-discovery - ${err}"
                                }
                            }
                            if (changes.any { it.startsWith("backend/LocketUserService/") }) {
                                echo "🔍 변경 감지됨: backend-user"
                                try {
                                    build job: 'backend-user'
                                    echo "✅ 빌드 성공: backend-user"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-user - ${err}"
                                }
                            }
                            if (changes.any { it.startsWith("backend/LocketPaymentService/") }) {
                                echo "🔍 변경 감지됨: backend-payment"
                                try {
                                    build job: 'backend-payment'
                                    echo "✅ 빌드 성공: backend-payment"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-payment - ${err}"
                                }
                            }
                            if (changes.any { it.startsWith("backend/LocketApiGateway/") }) {
                                echo "🔍 변경 감지됨: backend-api-gateway"
                                try {
                                    build job: 'backend-api-gateway'
                                    echo "✅ 빌드 성공: backend-api-gateway"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-api-gateway - ${err}"
                                }
                            }
                            if (changes.any { it.startsWith("backend/LocketElasticSearch/") }) {
                                echo "🔍 변경 감지됨: backend-elasticsearch"
                                try {
                                    build job: 'backend-elasticsearch'
                                    echo "✅ 빌드 성공: backend-elasticsearch"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-elasticsearch - ${err}"
                                }
                            }
                            if (changes.any { it.startsWith("backend/LocketAIService/") }) {
                                echo "🔍 변경 감지됨: backend-ai"
                                try {
                                    build job: 'backend-ai'
                                    echo "✅ 빌드 성공: backend-ai"
                                } catch (err) {
                                    echo "❌ 빌드 실패: backend-ai - ${err}"
                                }
                            }
                        }

                        // 2. 변경 감지 준비
                        sh 'git clean -fdx'
                        sh "git fetch https://${GIT_USERNAME}:${GIT_PASSWORD}@lab.ssafy.com/s12-fintech-finance-sub1/S12P21D204.git backend"

                        def parentCount = sh(script: "git rev-list --count --parents -n 1 HEAD | awk '{print NF-1}'", returnStdout: true).trim().toInteger()
                        def currentCommit = sh(script: "git rev-parse HEAD", returnStdout: true).trim()
                        def changes = []

                        // 3. 변경 파일 감지
                        if (parentCount > 1) {
                            def parent1 = sh(script: "git rev-parse HEAD^1", returnStdout: true).trim()
                            def parent2 = sh(script: "git rev-parse HEAD^2", returnStdout: true).trim()
                            def changes1 = sh(script: "git diff --name-only ${parent1} ${currentCommit}", returnStdout: true).trim().split('\n')
                            def changes2 = sh(script: "git diff --name-only ${parent2} ${currentCommit}", returnStdout: true).trim().split('\n')
                            changes = (changes1 + changes2).toSet().toList()
                            echo "📂 변경된 파일 목록 (merge): ${changes.join(', ')}"
                        } else {
                            def parentCommit = sh(script: "git rev-parse HEAD^1", returnStdout: true).trim()
                            changes = sh(script: "git diff --name-only ${parentCommit} ${currentCommit}", returnStdout: true).trim().split('\n')
                            echo "📂 변경된 파일 목록: ${changes.join(', ')}"
                        }

                        // ✅ 각 파일 경로 trim 처리 (공백 제거)
                        changes = changes.collect { it.trim() }
                        echo "🧪 Cleaned 파일 목록: ${changes.join(', ')}"

                        // 4. 빌드 트리거
                        triggerBuilds(changes)
                    }
                }
            }
        }
    }
}
```

## 외부 서비스 API
[CLOVA OCR API](https://www.ncloud.com/product/aiService/ocr)

[Perplexity API](https://docs.perplexity.ai/api-reference/chat-completions)

[ChatGPT API](https://openai.com/index/introducing-chatgpt-and-whisper-apis/)
